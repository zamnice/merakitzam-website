<!DOCTYPE html>
<html lang="id-ID" class="">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} | {% endif %}MerakitZAM</title>
    <meta name="description" content="{{ description | default('Pelajar ceria yang berbagi tips belajar produktif, desain kreatif, dan konten edukatif') }}">
    
    <!-- SEO Meta Tags -->
    <meta property="og:title" content="{% if title %}{{ title }} | {% endif %}MerakitZAM">
    <meta property="og:description" content="{{ description | default('Pelajar ceria yang berbagi tips belajar produktif') }}">
    <meta property="og:image" content="{{ thumbnail | default('https://merakitzam.my.id/assets/og-image.png') }}">
    <meta property="og:type" content="{% if page.url == '/' %}website{% else %}article{% endif %}">
    
    <!-- Styles -->
    <link rel="stylesheet" href="/css/globals.css">
    <link rel="stylesheet" href="/css/style.css">
    
    <!-- Favicon -->
    <link rel="icon" href="/assets/favicon.ico">
</head>
<body class="bg-background dark:bg-gray-900 text-text dark:text-gray-100 transition-colors duration-300">
    {% include "partials/nav.njk" %}
    
    <main>
        {{ content | safe }}
    </main>
    
    {% include "partials/footer.njk" %}
    
    <!-- Scripts -->
    <script src="/js/darkmode.js"></script>
    <script src="/js/i18n.js"></script>
    <script src="/js/script.js"></script>
    
    <!-- Decap CMS -->
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
</body>
</html>
